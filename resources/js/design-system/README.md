Design System (starter)

- Location: `resources/js/Components/ui`
- Purpose: centralize reusable UI primitives (Button, Input, etc.)
- CSS tokens: `resources/css/design-tokens.css`
- Usage: import components where needed, prefer Tailwind utility classes combined with `ui-` helpers.

Next steps:
- Add Storybook and visual tests
- Expand component props and variants
- Add form, select, modal primitives
